{
  "capabilities": [
    "http_relay"
  ],
  "description": "Parameters name which customer secrets hold the upstream URL and API key, so one offering can serve many distinct backends per customer.",
  "name": "byoe-params",
  "service_type": "proxy",
  "status": "ready",
  "summary": "BYOE echo relay with per-enrollment secret selection.",
  "time_created": "2026-04-18T00:00:00Z",
  "upstream_access_config": {
    "echo_service": {
      "access_method": "http",
      "api_key": "${ customer_secrets.{{ params.api_key_secret }} ?? }",
      "base_url": "${ customer_secrets.{{ params.base_url_secret }} }"
    }
  }
}
