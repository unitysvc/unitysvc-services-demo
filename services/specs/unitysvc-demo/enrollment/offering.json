{
  "capabilities": [
    "http_relay"
  ],
  "description": "Every enrollment's intrinsic 4-character `{{ enrollment.code }}` is substituted directly into both the customer-facing gateway URL and the upstream URL, giving every enrollment a unique path.",
  "name": "enrollment",
  "service_type": "proxy",
  "status": "ready",
  "summary": "Demonstrates per-enrollment URLs.",
  "time_created": "2026-04-18T00:00:00Z",
  "upstream_access_config": {
    "echo_service": {
      "access_method": "http",
      "base_url": "https://echo.unitysvc.dev/{{ enrollment.code }}"
    }
  }
}
