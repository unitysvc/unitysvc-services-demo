{
  "capabilities": [
    "smtp_relay"
  ],
  "description": "The customer connects their own SMTP server (Gmail, SendGrid, SES, etc.) through the UnitySVC gateway; host, port, username, and password are all resolved from the customer's secret store.",
  "name": "smtp-byoe",
  "payout_price": {
    "description": "No platform commission — customer uses own SMTP",
    "percentage": "100.00",
    "type": "revenue_share"
  },
  "service_type": "email",
  "status": "ready",
  "summary": "BYOE SMTP relay.",
  "time_created": "2026-04-18T00:00:00Z",
  "upstream_access_config": {
    "customer_smtp": {
      "access_method": "smtp",
      "host": "${ customer_secrets.DEMO_SMTP_RELAY_HOST }",
      "password": "${ customer_secrets.DEMO_SMTP_RELAY_PASSWORD }",
      "port": "${ customer_secrets.DEMO_SMTP_RELAY_PORT }",
      "tls": false,
      "username": "${ customer_secrets.DEMO_SMTP_RELAY_USERNAME }"
    }
  }
}
