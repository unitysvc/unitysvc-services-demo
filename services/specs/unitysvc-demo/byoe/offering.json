{
  "capabilities": [
    "http_relay"
  ],
  "description": "The customer supplies both the upstream endpoint URL and API key via customer secrets, so each customer routes to their own echo instance.",
  "name": "byoe",
  "service_type": "proxy",
  "status": "ready",
  "summary": "BYOE HTTP relay.",
  "time_created": "2026-04-18T00:00:00Z",
  "upstream_access_config": {
    "echo_service": {
      "access_method": "http",
      "api_key": "${ customer_secrets.ECHO_BYOE_API_KEY ?? }",
      "base_url": "${ customer_secrets.ECHO_BYOE_BASE_URL }"
    }
  }
}
